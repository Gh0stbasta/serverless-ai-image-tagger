{
  "name": "serverless-ai-image-tagger-root",
  "version": "1.0.0",
  "description": "Root management for AI Image Tagger Monorepo",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix infra && npm install --prefix backend",
    "test": "npm test --prefix infra",
    "build": "npm run build --prefix infra",
    "cdk": "npm run cdk --prefix infra --",
    "deploy": "npm run cdk --prefix infra -- deploy",
    "destroy": "npm run cdk --prefix infra -- destroy",
    "synth": "npm run cdk --prefix infra -- synth"
  }
}